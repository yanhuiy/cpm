# 全国算力看板 · 数据更新操作流程

本文档固化「数据更新」的标准操作流程，覆盖：AI 检索刷新、历史回填、质量体检、数据导出、前端验证。

---

## 0. 数据流总览

```
data.json（唯一主数据源）
   │
   │  python export_data.py
   ▼
data.js（前端数据层，自动生成，勿手改）
   │
   │  <script src="data.js">
   ▼
index.html（页面渲染）
```

> 核心原则：**只改 `data.json`**，`data.js` 一律由 `export_data.py` 生成。

---

## 1. 前置条件（仅首次）

- 安装 **Python 3**（≥3.8）与 **Node.js**（用于前端语法体检）
- 安装依赖：

```powershell
pip install requests
```

- 配置 AI 检索密钥（详见第 2 节）

---

## 2. AI 检索密钥配置（必读）

数据更新依赖两个密钥：

| 环境变量 | 必填 | 用途 | 获取地址 |
|---|---|---|---|
| `DEEPSEEK_API_KEY` | 是 | 结构化数据抽取 | https://platform.deepseek.com |
| `TAVILY_API_KEY` | 否（推荐） | 实时新闻检索 | https://tavily.com（免费约 1000 次/月） |

> 未配置 `TAVILY_API_KEY` 时退化为「纯模型知识模式」（时效性有限，可能漏最新资讯）。
> 未配置 `DEEPSEEK_API_KEY` 时，`refresh.py --run` 会**直接跳过刷新**（新增/更新均为 0）。

### 2.1 临时配置（当前终端会话，推荐日常使用）

```powershell
$env:DEEPSEEK_API_KEY = "sk-你的密钥"
$env:TAVILY_API_KEY   = "tvly-你的密钥"
```

### 2.2 永久配置（系统级，一次配置长期生效）

```powershell
setx DEEPSEEK_API_KEY "sk-你的密钥"
setx TAVILY_API_KEY   "tvly-你的密钥"
# 配置后需重开终端才会生效
```

### 2.3 验证配置是否生效

```powershell
python -c "import os; print('DEEPSEEK:', bool(os.environ.get('DEEPSEEK_API_KEY')), '| TAVILY:', bool(os.environ.get('TAVILY_API_KEY')))"
```

输出应为 `DEEPSEEK: True | TAVILY: True`。

> ⚠️ 密钥切勿写入代码或提交到 git。建议加入 `.gitignore`（见《部署与 Git 方案》）。

---

## 3. 月度更新（标准流程）

每月执行一次，用 AI 检索补充「新增建成/开工/投产/规划公示」的算力中心与政策。

```powershell
# 1. 执行 AI 检索刷新
python refresh.py --run

# 2. 一键体检（数据合规 + 前端语法）
python check_all.py

# 3. 硬刷新浏览器验证
#    Ctrl + F5 打开 index.html，检查各省标签、KPI、地图、近 1 月列表、图表、政策
```

`--run` 内部会自动完成：Tavily 检索 → DeepSeek 抽取 → 归一化 → 按 id 增量合并 → 写回 `data.json` → 调用 `export_data.py` 重新生成 `data.js`。

---

## 4. 历史回填（补充历史数据）

一次性批量回填某段时间的数据（按年查询数据消耗更小，按月更精确）。

```powershell
# 按年回填（默认，成本低）
python refresh.py --backfill 2020-01 2026-08

# 按月回填（更精确，API 消耗更大）
python refresh.py --backfill 2024-09 2026-08 --monthly
```

回填完成后，同样执行 `python check_all.py` 体检 + 硬刷新验证。

---

## 5. 质量体检（每次数据变动后必做）

```powershell
python check_all.py
```

检查项：

| 类别 | 检查内容 |
|---|---|
| 数据合规 | 量纲互斥（智算/超算/IDC/通用·云）、`id`-类型一致性、`vendor`/`province` 合法性、坐标范围、同名重复项、政策字段 |
| 前端语法 | `data.js` / `china_geo.js` / `cities_geo.js` / `echarts.min.js` / `index.html` 内联脚本 |

- 退出码 `0` = 全部通过；`1` = 有异常。
- 若报「数据合规」问题：直接改 `data.json` 对应字段，再 `python export_data.py`。
- 若报「前端语法」问题：多半是 `index.html` 内联脚本被破坏，回到 `index.html` 定位修复。

---

## 6. 归档与备份建议

- 每次成功回填/更新后，用 git 提交一个版本（见《部署与 Git 方案》），保留历史可追溯。
- 关键节点可手动备份 `data.json`。

---

## 7. 常见问题（FAQ）

| 现象 | 原因 | 处理 |
|---|---|---|
| `刷新完成：新增 0 / 更新 0` | 未配置 `DEEPSEEK_API_KEY` | 按第 2 节配置密钥后重跑 |
| `[fetch_latest] 检索失败` | `TAVILY_API_KEY` 无效/超额 | 检查密钥或降级为纯模型模式 |
| `UnicodeEncodeError` | Windows 终端编码 | 用 `python -X utf8 refresh.py --run` |
| 页面白屏/各省标签丢失 | `index.html` 内联脚本语法错误 | `python check_all.py` 定位并修复 |
| 图表报 `instance has been disposed` | ECharts 重复初始化 | 切换区域时先 `dispose` 再重建 |